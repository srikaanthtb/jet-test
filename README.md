JET fullstack test

This is a fullstack test for JET.
Using Node.js and Express for the backend, and React for the frontend.

Build insturctions

git clone https://github.com/srikaanthtb/jet-test.git
cd jet-test
docker compose up --build
open http://localhost:3000/
